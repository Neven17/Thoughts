<div class="card">

    <div class="card-body">
        <p class="fs-4"> <span class="{{$icon ?? 'fas fa-user'}}"></span> {{$title ?? ''}}</p> {{--ako je icon 0, --}}
        <h2 class="fw-bold fs-2">{{$data ?? 0}}</h2>{{-- ako nema date stavi nanulu --}}
    </div>

</div>